// evenly divisible means leaving no remainder when divided by
function smallestCommons(arr) {
    // get prime factors of both number
    let [a, b] = arr;
    
    function getPrimeFactors(num) {
        let primes = [];
        let j = 2;
        for (let i = 0; i <= Math.sqrt(num); i++) {
            if (num % primes[0] == 0 )
        }
    }
}

smallestCommons([1, 5]);